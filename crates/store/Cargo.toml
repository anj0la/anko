[package]
name = "store"
version = "0.1.0"
edition = "2024"

[dependencies]
firestore = "0.50.0"
serde = { version = "1.0.229", features = ["derive"] }
tokio = { version = "1.53.1", features = ["full"] }
scanner = { path = "../scanner" }